*.iml
.gradle
/local.properties
# IDE state is not source: ignore the whole folder rather than single files.
/.idea/
.DS_Store
# Anchored at the root this would miss core/build and app/build.
build/
/captures
.externalNativeBuild
.cxx
local.properties
