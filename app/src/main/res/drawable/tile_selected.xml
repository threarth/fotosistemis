<?xml version="1.0" encoding="utf-8"?>
<!-- Chosen but not yet decided: a thicker, darker edge, so "I am about to
     act on these" never looks like "these are done". -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:width="5dp"
        android:color="@color/ink" />
</shape>
